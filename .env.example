SIH_DATA_ROOT=data/projects

# Aggregate HTTP upload limit (video + telemetry + multipart overhead): 4 GiB.
SIH_MAX_UPLOAD_BYTES=4294967296
# Keep at least 1 GiB available; allow additional space for reconstruction outputs.
SIH_MIN_FREE_DISK_BYTES=1073741824
UVICORN_HOST=127.0.0.1
