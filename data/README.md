# Data policy

Raw video, telemetry, generated models, model weights, and sensitive mission data belong under
`data/projects/` and are intentionally ignored by Git. Commit only schemas, small approved fixtures,
hashes, and non-sensitive evidence.

